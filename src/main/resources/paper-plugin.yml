name: AsureCore
version: '1.0'
author: SaintQd
main: org.saintqd.asurecore.AsureCore
description: AsureCore Plugin
api-version: '26.2'
dependencies:
  server:
    AsureLib:
      load: BEFORE
      required: true
    PlaceholderAPI:
      load: BEFORE
      required: false
    Vault:
      load: BEFORE
      required: false
    CMI:
      load: BEFORE
      required: false
    WorldGuard:
      load: BEFORE
      required: false
    LiteBans:
      load: BEFORE
      required: false
    LuckPerms:
      load: BEFORE
      required: false
    Nexo:
      load: BEFORE
      required: false
    MythicMobs:
      load: BEFORE
      required: false
    HMCCosmetics:
      load: BEFORE
      required: false